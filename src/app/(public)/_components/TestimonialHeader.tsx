import { SectionHeader } from "@/components/shared/SectionHeader";

export const TestimonialHeader = () => {

  return (
    <SectionHeader title="What Our Customers Say" subtitle="Real stories from homeowners like you." />
  );
}